import { Container, Typography } from "@mui/material";

const Home = (): JSX.Element => {
  return (
    <Container>
      <Typography>Dashboard</Typography>
    </Container>
  );
};

export default Home;
