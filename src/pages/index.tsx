import { signUp, signIn } from '../lib/auth';

const Home = () : JSX.Element => {
  // const createAccount = signUp("hoge@hoge.hogea", "hogehogea");
  // const logIn = signIn("hoge@hoge.hogea", "hogehogea");

  return (
    <div>
      Hello, World!
    </div>
  )
}

export default Home
